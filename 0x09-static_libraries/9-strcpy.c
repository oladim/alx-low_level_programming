#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * _strcpy - function to copy
 *
 * @dest: array parameter
 * @src: number of occurence
 *
 * Return: this returns a copy of source to calling program
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
	printf("\n");
}
