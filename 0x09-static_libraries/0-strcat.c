#include "main.h"
#include <string.h>

/**
 * _strcat - creating copy funvtion
 * @dest: first param
 * @src: second param
 * Return: result of copy
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
