#include "main.h"

/**
 * main - entry point
 * @argc: number of arguments from the prompt
 * @argv: array of arguments
 * Return: Always 0 Success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
