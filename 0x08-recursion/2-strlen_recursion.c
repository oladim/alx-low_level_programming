#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - function using recursion to print
 * @s: param
 * Return: length of a string
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
